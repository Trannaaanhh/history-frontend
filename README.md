# Vietnamese History Learning Platform

A client-side React application providing structured lessons, quizzes, progress tracking, and a simulated retrieval-assisted history chat experience.

Run locally with `pnpm install` followed by `pnpm dev`.
